# ADR 2: Kubernetes distribution

Production uses k3s because it has a smaller resource footprint than a full
kubeadm installation and remains conformant with Kubernetes APIs used by Helm
and Argo CD. The production cluster runs on Proxmox virtual machines.

Local development uses the Kubernetes cluster integrated into Docker Desktop.
This keeps the workstation setup limited to Docker Desktop.

